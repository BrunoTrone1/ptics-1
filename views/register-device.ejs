<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Registrar un Dispositivo</title>
    <!-- Bootstrap -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <!-- Iconos -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />
    <!-- Estilos personalizados -->
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body class="d-flex flex-column min-vh-100">
    <!-- Barra de navegación -->
    <header class="navbar navbar-dark bg-dark">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">
          <h1><i class="fas fa-shield-alt"></i> Safe Device</h1>
        </a>
        <nav class="navbar navbar-expand-lg">
          <!-- Links para las demas rutas -->
          <div class="collapse navbar-collapse justify-content-end">
            <ul class="navbar-nav">
              <% if (!isLoggedIn) { %>
              <li class="nav-item">
                <a class="nav-link" href="/register">Registrarse</a>
              </li>
              <% } %> <% if (isLoggedIn) { %>
              <li class="nav-item">
                <a class="nav-link" href="/register-device"
                  >Registrar Dispositivo</a
                >
              </li>
              <% } %> <% if (!isLoggedIn) { %>
              <li class="nav-item">
                <a class="nav-link" href="/login">Iniciar Sesión</a>
              </li>
              <% } %> <% if (isLoggedIn) { %>
              <li class="nav-item">
                <a class="nav-link" href="/view-devices">Ver Dispositivos</a>
              </li>
              <% } %><% if (isLoggedIn) { %>
              <li class="nav-item">
                <a class="nav-link" href="/logout">Salir</a>
              </li>
              <% } %>
            </ul>
          </div>
        </nav>
      </div>
    </header>

    <div class="form-container">
      <div class="welcome-message">
        <h1 class="display-4">Registrar Dispositivo</h1>
        <p class="lead">
          Por favor, ingrese los detalles del dispositivo para registrarlo en el
          sistema.
        </p>
      </div>

      <!-- Formulario de registro de dispositivo -->
      <form action="/register-device" method="POST">
        <div class="mb-3 form-group">
          <label for="deviceId" class="form-label"
            >ID del Dispositivo <i class="fas fa-key"></i
          ></label>
          <input
            type="text"
            class="form-control"
            id="deviceId"
            name="deviceId"
            placeholder="Ingrese el ID del dispositivo"
            required
          />
        </div>
        <div class="mb-3 form-group">
          <label for="deviceId" class="form-label"
            >Nombre del Dispositivo <i class="fas fa-mobile-alt"></i
          ></label>
          <input
            type="text"
            class="form-control"
            id="deviceName"
            name="deviceName"
            placeholder="Ingrese el Nombre del dispositivo"
            required
          />
        </div>
        <button type="submit" class="btn btn-primary btn-lg btn-body">
          Registrar
        </button>
      </form>
    </div>

    <!-- Pie de página -->
    <div class="footer py-3 text-white mt-auto">
      <div class="container text-center">
        <p>&copy; 2024 Safe Device. Todos los derechos reservados.</p>
      </div>
    </div>

    <!-- Integración de Bootstrap JS (si es necesario) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
